<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools" xmlns:ns1="http://schemas.android.com/apk/res-auto">

    <style name="ToolbarTitleStyle" parent="Widget.AppCompat.Toolbar">
        <item name="titleTextAppearance">@style/ToolbarTitleText</item>
        <item name="subtitleTextAppearance">?attr/textAppearanceTitleMedium</item>
        <item name="contentInsetStartWithNavigation">0dp</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>

        <!-- Pushes the content (Navigation Icon) down -->
        <item name="android:paddingTop">25dp</item>
        <!-- Ensure gravity is top so padding takes effect from top -->
        <item name="android:gravity">top</item>

        <!-- Additional top margin for the Title -->
        <item name="titleMarginTop">10dp</item>


        <item name="android:outlineAmbientShadowColor" ns1:targetApi="p" tools:targetApi="28">
            @android:color/transparent
        </item>
        <item name="android:outlineSpotShadowColor" ns1:targetApi="p" tools:targetApi="28">@android:color/transparent
        </item>
    </style>

    <style name="ToolbarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/chakra_petch_bold</item>
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="barLength">30dp</item>
        <item name="thickness">4dp</item>
        <item name="gapBetweenBars">5dp</item>
        <item name="color">@color/white</item>
    </style>

    <!-- Shape con solo esquinas superiores redondeadas -->
    <style name="ShapeTopRounded" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <!-- Pon 0dp por defecto y luego especifica solo las de arriba -->
        <item name="cornerSize">0dp</item>

        <item name="cornerSizeTopLeft">25dp</item>
        <item name="cornerSizeTopRight">25dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

</resources>